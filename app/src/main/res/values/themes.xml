<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- ОСНОВНАЯ ТЕМА ПРИЛОЖЕНИЯ -->
    <style name="Theme.Sudoky" parent="Theme.AppCompat.DayNight.NoActionBar" />

    <!-- СИСТЕМНЫЙ SPLASH: ЧЁРНЫЙ ЭКРАН БЕЗ ИКОНКИ -->
    <style name="Theme.Sudoky.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_empty</item>
        <item name="postSplashScreenTheme">@style/Theme.Sudoky</item>
    </style>

</resources>

